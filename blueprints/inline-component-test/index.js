var componentTestBlueprint = require('ember-source/blueprints/component-test');
var blueprint = Object.assign({}, componentTestBlueprint);

blueprint.description = 'Generates a inline component integration or unit test.';

module.exports = blueprint;